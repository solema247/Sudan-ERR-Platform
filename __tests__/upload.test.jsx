const fs = require("fs");
const path = require("path");

describe('JSON test', () => {
    it('checks that you can upload files to buckets and they will be tracked in the DB', () => {
      
    })  
})
